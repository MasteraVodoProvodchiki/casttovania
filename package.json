{
	"name": "@masteravodoprovodchiki/casttovania",
	"version": "0.0.0",
	"description": "Convert string to something",
	"main": "./dist/index.js",
	"repository": "https://github.com/MasteraVodoProvodchiki/casttovania.git",
	"author": "evilfant <evilfant@gmail.com>",
	"license": "MIT",
	"private": false,
	"files": [
		"/dist"
	],
	"scripts": {
		"build": "yarn build:clean && yarn build:lib",
		"build:lib": "cross-env NODE_ENV=production tsc",
		"build:clean": "rimraf ./dist",
		"test": "yarn test:only",
		"test:only": "jest --coverage",
		"test:watch": "jest --watch",
		"lint": "yarn lint:tslint && yarn lint:eslint",
		"lint:tslint": "tslint --project ./",
		"lint:eslint": "eslint ./",
		"bump": "yarn version --patch --non-interactive --message \"v%s [skip ci]\"",
		"publish": "yarn build && yarn info"
	},
	"devDependencies": {
		"@types/jest": "^24.0.11",
		"cross-env": "^5.2.0",
		"eslint": "^5.16.0",
		"eslint-config-node": "^4.0.0",
		"jest": "^24.7.1",
		"ts-jest": "^24.0.2",
		"tslint": "^5.16.0",
		"tslint-config-airbnb": "^5.11.1",
		"typescript": "^3.4.3"
	}
}
